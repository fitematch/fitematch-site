import { EmptyState } from '@/components/ui/empty-state';

export function SessionList() {
  return (
    <EmptyState message="A listagem de sessões será exibida aqui quando o endpoint estiver disponível." />
  );
}
