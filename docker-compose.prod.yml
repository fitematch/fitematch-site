services:
  fitematch-site:
    container_name: fitematch-site
    image: fitematch-site:production
    build:
      context: .
      target: runner
    ports:
      - '3001:3000'
    env_file:
      - .env.production
    restart: unless-stopped
